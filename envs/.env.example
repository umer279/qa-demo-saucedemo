# Copy to envs/.env.local (or use committed envs/*.env files in this demo)

BASE_URL=https://www.saucedemo.com/

# Timeouts (milliseconds)
TEST_TIMEOUT=60000
EXPECT_TIMEOUT=10000
ACTIONS_TIMEOUT=30000

# debug | info | warn (debug enables env warnings in console)
LOG_LEVEL=info
